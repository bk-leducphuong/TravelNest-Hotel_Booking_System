apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: travelnest
resources:
  - namespace.yaml
  - ../../infra/mysql/overlays/local
  - ../../infra/redis
  - ../../infra/nats
  - ../../infra/keycloak/overlays/local
  - ../../infra/minio/overlays/local
  # Dev-only data stores (managed MongoDB/Elasticsearch in prod).
  - ../mongodb
  - ../elasticsearch

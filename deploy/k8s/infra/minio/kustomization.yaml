apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: travelnest
resources:
  - service.yaml
  - deployment.yaml
  - pvc.yaml
  - ingress.yaml

generators:
  - secret-generator.yaml

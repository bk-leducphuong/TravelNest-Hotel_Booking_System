// https://nuxt.com/docs/api/configuration/nuxt-config
export default defineNuxtConfig({
  compatibilityDate: "2025-07-15",
  devtools: { enabled: true },
  runtimeConfig: {
    public: {
      apiBase:
        process.env.NUXT_PUBLIC_API_BASE || "http://localhost:3000/api/v1",
    },
  },
  modules: [
    "@element-plus/nuxt",
    "@nuxt/eslint",
    "@pinia/nuxt",
    "@nuxtjs/tailwindcss",
    "@nuxt/test-utils/module",
  ],
  elementPlus: {
    /** Options */
  },
  pinia: {
    storesDirs: ["stores"],
  },
  tailwindcss: {
    exposeConfig: true,
    viewer: true,
    // and more...
  },
  vite: {
    server: {
      watch: {
        usePolling: false,
        ignored: [
          // Ignore files that are not in use, such as .gitignore
          "**/!(.gitignore)",
          // Ignore files that are generated by Nuxt
          "**/@(.output|node_modules|.nuxt|dist)",
        ],
      },
    },
  },
  devServer: {
    port: 8000,
  },
});
